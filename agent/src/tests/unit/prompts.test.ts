import { describe, it } from "vitest";

describe("Prompt registry", () => {
  it.todo("v1 prompt template contains required variables");
  it.todo("v2 prompt template contains required variables");
  it.todo("judge prompt returns structured score");
});
