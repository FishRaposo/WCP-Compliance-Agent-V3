import { describe, expect, it, vi } from "vitest";

describe("Mastra tools", () => {
  it.todo("extract tool forwards text to Python /extract");
  it.todo("validate tool forwards extracted WCP to Python /validate");
  it.todo("dbwd_lookup tool calls Python /dbwd/{trade}/{locality}/{date}");
  it.todo("search tool calls Python /search with correct payload");
  it.todo("persist tool POSTs decision to Python /decisions");
});
